    </div>

<!DOCTYPE html>
<html lang="en" class="dark">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Email Verification</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://js.hcaptcha.com/1/api.js" async defer></script>
    <link rel="icon" href="https://cdn-icons-png.flaticon.com/512/5968/5968756.png" />
    <style>
      body, html { overflow-x: hidden; }
    </style>
  </head>
  <body class="bg-gradient-to-br from-gray-900 via-gray-800 to-gray-900 text-white font-sans min-h-screen flex items-center justify-center px-4">

    <div class="bg-gray-800/90 border border-gray-700 rounded-3xl shadow-2xl w-full max-w-md p-8 sm:p-10 backdrop-blur-md">
      <div class="text-center mb-8">
        <img
          src="https://cdn-icons-png.flaticon.com/512/5968/5968756.png"
          alt="Discord Logo"
          class="mx-auto w-20 h-20 mb-4 animate-bounce"
        />
        <h1 class="text-3xl font-extrabold mb-2 text-indigo-300 flex items-center justify-center gap-2">
          <span>Discord Email Verification</span>
        </h1>
        <p class="text-gray-300 text-base">Please verify before continuing with Discord</p>
      </div>

      <% if (error) { %>
        <div class="bg-red-600/90 text-white text-base px-4 py-2 rounded-xl mb-4 text-center font-semibold flex items-center gap-2 justify-center shadow">
          <span>❌</span> <%= error %>
        </div>
      <% } %>

      <% if (success) { %>
        <div class="bg-green-600/90 text-white text-base px-4 py-2 rounded-xl mb-4 text-center font-semibold flex items-center gap-2 justify-center shadow">
          <span>✅</span> <%= success %>
        </div>
      <% } %>


      <form action="/captcha" method="POST" class="space-y-6">

        <div class="flex justify-center">
          <div class="w-full flex justify-center">
            <div class="h-captcha mx-auto" data-sitekey="<%= siteKey %>" style="min-width: 100px; max-width: 100%; width: 100%; display: flex; justify-content: center;"></div>
          </div>
        </div>

        <button
          type="submit"
          class="w-full bg-gradient-to-r from-indigo-500 to-purple-500 hover:from-indigo-400 hover:to-purple-400 transition-all duration-150 py-2 px-6 rounded-xl font-bold shadow-lg flex items-center justify-center gap-2 text-lg"
        >
          <span>✅</span> Verify to Continue
        </button>
      </form>

      <p class="text-xs text-center text-gray-500 mt-8 flex items-center justify-center gap-1">
        <img src="https://cdn-icons-png.flaticon.com/512/5968/5968756.png" class="w-4 h-4" />
        <span>2025 Discord Verification</span>
      </p>
    </div>
  </body>
</html>
